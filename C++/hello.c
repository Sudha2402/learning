#include<stdio.h>

int main()
{

    printf("hello\n");
    return 0;
    
}


/*
PS C:\Sudhadocuments\C++> gcc hello.c -o hello
PS C:\Sudhadocuments\C++> ./hello  
hello


gcc test.c -o test
test.exe

./hello.exe

Only this way works but direct run terminal is not working with gcc


Run the Program Manually (Optional):
After compiling (gcc -g hello.c -o hello.exe), you can manually debug with:

sh
gdb ./hello.exe


>./hello.exe
it works when .exe file exist



direct execution shows in 

Click the "Run and Debug" icon in the left sidebar (or press Ctrl+Shift+D).

In the top dropdown, select "Run C++ Program (No Debug)" (not the debug configuration).

*/