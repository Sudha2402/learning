#include <stdio.h>

int main() {


      //  struct strp *ptr_str = &x; // NOTE:as datatype to be same as value hance  struct strp is used as datatype before pointer


      //  void (*ptr_func)(int, int) = &func;
       void *ptr_vn = NULL;
       int *ptr_int = NULL;


    printf("");
    printf("\n");
      
      return 0;
}
 
 
 

